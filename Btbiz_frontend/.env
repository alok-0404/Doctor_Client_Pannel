# Shared hints only. Real values:
# - Local dev → .env.local (not committed)
# - Production build → .env.production (HTTPS Replit / deploy URL)

# VITE_API_BASE_URL=http://localhost:4000
# If backend PORT is not 4000, set both in .env.local:
# VITE_DEV_PROXY_TARGET=http://localhost:YOUR_PORT
